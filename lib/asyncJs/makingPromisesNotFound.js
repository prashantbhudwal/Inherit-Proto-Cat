const makingPromisesNotFound = function () {
  //I couldn't find anything without set timeout or wrapping something as a promise that's already a promise, like fetch.
  console.log(
    "After 2 hours of research I have found no place where I can realistically use the Promise constructor in a real world scenario. I will come back to this later, if needed."
  );
};

export default makingPromisesNotFound;
